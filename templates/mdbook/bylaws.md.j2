{#
 # Template for SUMMARY.md for an mdBook
 #
 # This functions as an index for the mdBook.
 # A regulation is put in a deeper level in the hierarchy if it has a dot in the id. It goes one level deeper for every character more than 2 after the dot. #}
# VSETH Rechtssammlung

<a href="/{{bylaws.filename}}.pdf" target="_blank" rel="noopener noreferrer">
    <i class="fa fa-file"></i> PDF-Version
</a>
<div class="right">Stand: {{ bylaws.date_last_update | format_date }}</div>

Willkommen in der Rechtssammlung des VSETH! Hier finden Sie die Statuten sowie alle Reglemente
und Verordnungen des VSETH.

{% for regl in bylaws.regulations %}
{% set parts = regl.id.split('.') %} 
{% set level = parts[1]|length %} 
{% set level = level - 1 %} 
{% set spaces = '   ' * level %} 
{{spaces}}- [{{regl.id}} {{regl.title}}]({{regl.filename}}.md)
{% endfor %}